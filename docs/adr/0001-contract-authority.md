# ADR 0001: Contract and execution authority

Status: accepted

`GOAL.md` defines success. JSON Schemas define wire data, OpenAPI defines HTTP exposure, and `config/execution_graph.yaml` defines execution. Documentation and generated views cannot override them. This removes the package's conflicting Core-only prompt and duplicated gate/packet manifests.
