gate: G0
status: IMPLEMENTATION_COMPLETE_EXTERNAL_PROOF_DEFERRED
blocker_class: external_runtime
blocker:
  component: WSL Ubuntu / PostgreSQL 16 / pgvector
  exact_failure: >-
    WSL CLI is responsive and reports default version 2, but no Linux
    distribution is registered. Previous web-download and Store-backed WSL
    installation attempts did not leave a runnable Ubuntu distribution. The
    bounded offline recovery cannot begin because no .wsl package or checksum
    exists anywhere under C:\\Users\\Rakesh. The last root probe returned
    Wsl/Service/WSL_E_DISTRO_NOT_FOUND.
attempts_completed:
  - command: wsl --install --web-download -d Ubuntu-24.04
    result: exited without a durable registered distribution
  - command: wsl --install -d Ubuntu-24.04
    result: stalled without a durable registered distribution or AppX package
  - command: wsl -d Ubuntu-24.04 -u root -- /bin/echo WSL_ROOT_OK
    result: Wsl/Service/WSL_E_DISTRO_NOT_FOUND
  - command: bounded offline .wsl package discovery under C:\\Users\\Rakesh
    result: no .wsl package or checksum file found
proven_requirements:
  - adapter-level evidence, extraction, lifecycle, retrieval, pack, runtime,
    evaluation, trace, and Studio evidence documented in docs/completion-audit.md
deferred_requirements:
  - live PostgreSQL migration apply/rollback/reapply
  - pgvector-backed retrieval
  - PostgreSQL RLS isolation
  - concurrent database idempotency
  - independent connection visibility
  - PostgreSQL restart persistence
  - clean PostgreSQL-backed installation proof
resume_condition: usable PostgreSQL 16 + pgvector runtime
